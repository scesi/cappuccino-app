name: test-action
on:
  pull_request:
    branches: ["develop"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm run test
    
    
